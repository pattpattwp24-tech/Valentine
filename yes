body {
  background: linear-gradient(135deg, #ffd1dc, #fff0f5);
  text-align: center;
  font-family: Arial, sans-serif;
  overflow: hidden;
}

.title {
  font-size: 60px;
  margin-top: 80px;
}

.message {
  font-size: 24px;
  line-height: 1.6;
}
